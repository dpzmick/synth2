(define create (config)
  (do
    ; add components
    (add-component (new SineWaveOscillatorConfig :name "sine"))

    ; connect components
    ; (connect '("voice" "frequency_out") '("sine" "frequency_in"))
    ; (connect '("sine" "samples-out")    '("voice" "samples-in"))
    ))
